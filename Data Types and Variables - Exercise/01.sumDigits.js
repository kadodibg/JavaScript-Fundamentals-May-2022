function sumDigits(num) {
    let sum = 0;
    let numberAsString = num.toString();
    // for (const char of numberAsString) {
    //     sum += Number(char);
    // }
    console.log(sum);
}
sumDigits(245678); 